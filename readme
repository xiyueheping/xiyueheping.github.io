- 所有本地数据
remember     备忘录数据
search       默认搜索引擎

http_data    所有网址信息

- 所有功能模块

主入口模块
事件模块
系统时间模块
网址模块
地点天气模块
备忘录模块
翻译模块
雪花效果模块

- 网址模块
  在本模块作用域中保存一个默认网址信息对象http_data
  在加载页面时，首先判断本地信息中有没有网址信息，如果没有把默认网址写入本地，再从本地更新到模块对象中
                如果本地有网址信息，直接把本地数据更新到模块对象中。
                然后根据模块对象渲染页面中所有网址以及分类信息
  点击添加网址时，首先判断网址的格式，没有问题再读取本地网址信息进行比对，并且记录新网址所在分类。
                如果没有空间不足，网址同名的现象就把新网址添加到全局对象中，把全局对象更新到本地，并
                根据新网址所在分类对页面重新渲染。
  点击删除网址时，从本地读取所有网址信息，找到需要删除的数据进行删除。
                 再根据颜色寻找当前页面所在分类
                 把全局对象更新到本地中，并且根据当前分类重新渲染页面。
  点击网址标题切换分类，根据颜色获取需要切换的分类，根据需要切换的分类对网址标题和网址内容重新渲染。

- 地点天气模块
  在本模块作用域中保存有当前城市位置和当前天气信息两个变量
  在加载页面时，根据腾讯地图接口请求城市位置信息，如果失败直接返回。
                如果请求成功，把请求的城市位置保存到模块变量中，然后根据城市地点请求当前城市天气信息
                              并且把天气信息更新到模块变量中
                              根据地点天气模块变量渲染页面，结束
  点击天气详情按钮， 打开天气详情窗口，根据模块中的天气变量渲染天气详情窗口
  点击天气详情关闭按钮，将天气详情窗口隐藏